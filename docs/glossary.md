Not an ECS system, but adapt and borrow concepts as we move along, as we see the need and fit

### Nodes
  - composed of entity/ies, we register entities here as well, and may contain 2D and View(Graphic)
  - could be systems as well, so we include the system in the tree
### Entity - entity_id, component/s
### System 
  - work with components, dispatch signals, react?
### Component
  - data + local level processing of the data, can't see much outside

